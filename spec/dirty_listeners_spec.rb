require 'spec_helper'

describe DirtyListeners do
  it 'has a version number' do
    expect(DirtyListeners::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
